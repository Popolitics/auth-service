DJANGO_SECRET_KEY=change-me
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
# DATABASE_URL=postgres://user:password@localhost:5432/auth_service

# Partagée avec les services qui doivent vérifier nos JWT (ex. data-service) —
# même valeur des deux côtés, distincte de DJANGO_SECRET_KEY.
JWT_SIGNING_KEY=change-me-too-and-share-with-services-that-verify-our-tokens
